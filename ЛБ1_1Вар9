#define _USE_MATH_DEFINES
#include <iostream>
#include <clocale>
#include <cmath>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Ukrainian");
    double R = 0.0;
    const double PI = M_PI;
    cout << "Ввести радіус кола" << endl;
    cin >> R;
    cout << "R = " << R << endl;
    double V = (4.0 / 3.0) * PI * R * R * R;
    cout << "V = " << V << endl;
    return 0;
}

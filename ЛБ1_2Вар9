#define _USE_MATH_DEFINES
#include <iostream>
#include <cmath>
#include <clocale>   

using namespace std;    

int main()
{
	setlocale(LC_ALL, "Ukrainian");
	double a = 0.0;
	double b = 0.0;	
	double c = 0.0;
    cout << "Введiть a: ";
    cin >> a;
    cout << "Введiть b: ";
    cin >> b;
    cout << "Введiть c: ";
    cin >> c;
    if ((b - a) == (c - b)) {
        cout << "Числа є членами арифметичної прогресiї V" << endl;
    }
    else {
        cout << "Числа не є членами арифметичної прогресiї X" << endl;
	}   
}
